namespace Test_DrinksV4.Lib
{
    public class TypeSearchCocktail
    {
        public enum Types
        {
            BY_NAME,
            BY_FIRST_LETTER,
            BY_ID,
            BY_RANDOM,
            BY_INGREDIENT,
            BY_ALCOHOL_FILTER,
            BY_CATEGORY,
            BY_GLASS
        }
    }
}
